//Variables
let nombre;
let apellido;
let usuario;
let diaReserva;
let asistentes;
let cuotasTarjeta;
let precioPlExc;
//Constantes
const recargoRes = 7;
const descTarjetaAmEx = 10;
const descTarjetaGalicia = 7;

//Cantidad de personas que asistirán
asistentes = parseFloat(prompt("¿cuántas personas asistirán?"));
//Nombre y apellido
nombre = prompt("para realizar la reserva, te vamos a pedir un nombre");
apellido = prompt("introducí tu apellido");
usuario = nombre + apellido;
//Día de la reserva
diaReserva = parseFloat(prompt("ingresá en formato dd/mm/aa el día que deseas reservar"));
//recargo por reserva que se descuenta del precio de lo que se consuma en el local
alert
//plato exclusivo que se puede reservar
//descuento con distintas tarjetas/efectivo / cuotas